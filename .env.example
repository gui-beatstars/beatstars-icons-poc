# Figma API Configuration
# Get your token at: https://www.figma.com/developers/api#access-tokens
FIGMA_TOKEN=your-figma-personal-access-token

# The file key from your Figma URL
# e.g. if URL is figma.com/design/aBcDeFgHi/Icons then key is "aBcDeFgHi"
FIGMA_FILE_KEY=your-figma-file-key

# The node ID of the frame/page containing your icons
# Select the frame in Figma → right-click → "Copy link" → extract node-id param
# e.g. "123:456"
FIGMA_ICON_FRAME_ID=123:456

# Optional: only export icons whose Figma name starts with this prefix
# e.g. "icon/" to match "icon/play", "icon/pause", etc.
# ICON_NAME_PREFIX=icon/
